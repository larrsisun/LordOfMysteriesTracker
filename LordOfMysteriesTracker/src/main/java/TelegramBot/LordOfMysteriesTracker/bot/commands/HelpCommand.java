package TelegramBot.LordOfMysteriesTracker.bot.commands;


import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.methods.send.SendMessage;

@Component
public class HelpCommand implements Command {

    @Override
    public void execute(Long chatID, String[] args, SendMessage response) {
        response.setText("""
            🤖 *Lord of the Mysteries Tracker Bot*
            
            📌 *Основные команды:*
            /start - Начать работу с ботом
            /subscribe - Подписаться на обновления
            /unsubscribe - Отписаться
            
            🎨 *Управление фильтрами:*
            /filter - Настройка фильтров (просмотр и замена)
            /addfilter - Добавить фильтры к текущим
            /removefilter - Удалить фильтры
            /clearfilters - Очистить все фильтры (вернуть "all")
            /showfilters - Показать все доступные фильтры
            
            📝 *Примеры использования фильтров:*
            • `/addfilter fanart discussion` - получать арты и обсуждения
            • `/addfilter theory` - добавить теории к текущим фильтрам
            • `/removefilter discussion` - перестать получать обсуждения
            • `/clearfilters` - сбросить все фильтры (получать всё)
            • `/filter fanart meme` - заменить фильтры на арты и мемы
            
            ⚙️ *Справка по фильтрам:*
            • Фильтр `all` включает все типы контента
            • При добавлении любого фильтра (кроме `all`) фильтр `all` автоматически отключается
            • Если удалить все фильтры, автоматически включается `all`
            
            📊 *Другие команды:*
            /stats - Статистика бота (если реализовано)
            /posts - Последние посты (если реализовано)
            
            ❓ *Проблемы?*
            Если что-то не работает, попробуйте:
            1. Переподписаться `/unsubscribe`, затем `/subscribe`
            2. Очистить фильтры `/clearfilters`
            3. Написать разработчику
            
            🔔 Бот проверяет новые посты каждые 5 минут.
            """);
    }

    @Override
    public String getName() {
        return "/help";
    }
}
