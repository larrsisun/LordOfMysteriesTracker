package TelegramBot.LordOfMysteriesTracker.bot.commands;


import org.springframework.stereotype.Component;
import org.telegram.telegrambots.meta.api.methods.send.SendMessage;

@Component
public class HelpCommand implements Command {

    @Override
    public void execute(Long chatID, String[] args, SendMessage response) {
        response.setText("""
            
            *Основные команды:*
            /start - Начать работу с ботом
            /subscribe - Подписаться на обновления
            /unsubscribe - Отписаться
            
            *Управление фильтрами:*
            /filter - Настройка фильтров (просмотр и замена)
            /filter add - Добавить фильтры к текущим
            /filter remove - Удалить фильтры
            /filter clear - Очистить все фильтры (вернуть "all")
            /filter list - Показать все доступные фильтры
            
            *Примеры использования фильтров:*
            • `/filter add fanart discussion` - получать арты и обсуждения
            • `/filter add theory` - добавить теории к текущим фильтрам
            • `/filter remove discussion` - перестать получать обсуждения
            • `/filter clear - сбросить все фильтры (получать всё)
            • `/filter fanart meme` - заменить фильтры на арты и мемы
            
            *Справка по фильтрам:*
            • Фильтр `all` включает все типы контента
            • При добавлении любого фильтра (кроме `all`) фильтр `all` автоматически отключается
            • Если удалить все фильтры, автоматически включается `all`
            
            
            *Проблемы?*
            Если что-то не работает, попробуйте:
            1. Переподписаться `/unsubscribe`, затем `/subscribe`
            2. Очистить фильтры `/filter clear`
            3. Написать разработчику @seosane
            
            🔔 Бот проверяет новые посты каждые 5 минут.
            """);
    }

    @Override
    public String getName() {
        return "/help";
    }
}
